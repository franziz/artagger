from distutils.core import setup
setup(
  name = 'tagger',
  packages = ['tagger'], # this must be the same as the name above
  version = '0.1',
  description = 'RDRPOSTagger implementation',
  author = 'Frans Huang',
  author_email = 'franssiswanto@gmail.com',
  url = 'https://github.com/franziz/tagger', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
  keywords = ['rdr', 'pos tagger', 'pos_tags'], # arbitrary keywords
  classifiers = [],
)